{{app("k8s")}}:
    base: ubuntu@{{ base }}
    channel: {{ channel }}
    revision: {{ k8s_revision }}
{{app("k8s-worker")}}:
    base: ubuntu@{{ base }}
    channel: {{ channel }}
    revision: {{ k8s_worker_revision }}
{{app("ceph-csi")}}: {}
{{app("ceph-mon")}}: {}
{{app("ceph-osd")}}: {}
